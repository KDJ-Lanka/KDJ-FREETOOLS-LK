import type { Metadata } from "next";
import Client from "./Client";

export const metadata: Metadata = {
  title: "Merge Audio Files Online Free — Combine Multiple Audio Files | FreeTools.lk",
  description: "Concatenate multiple audio files into one MP3. Browser-based, no upload.",
  keywords: ["merge audio","combine audio","audio merge"],
};

export default function Page() { return <Client />; }

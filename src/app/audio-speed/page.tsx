import type { Metadata } from "next";
import Client from "./Client";

export const metadata: Metadata = {
  title: "Change Audio Speed Online Free — Speed Up or Slow Down Audio | FreeTools.lk",
  description: "Speed up or slow down audio without changing the pitch. Browser-based.",
  keywords: ["audio speed","change audio speed","atempo"],
};

export default function Page() { return <Client />; }

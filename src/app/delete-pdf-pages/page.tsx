import type { Metadata } from "next";
import Client from "./Client";
export const metadata: Metadata = { title: "Delete PDF Pages – FreeTools.lk", description: "Remove specific pages from a PDF quickly and easily." };
export default function Page() { return <Client />; }

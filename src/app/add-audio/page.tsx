import type { Metadata } from "next";
import Client from "./Client";

export const metadata: Metadata = {
  title: "Add Audio to Video Online Free — Replace Audio Track | FreeTools.lk",
  description: "Replace or add an audio track to any video. Browser-based, no upload required.",
  keywords: ["add audio","replace audio","audio track"],
};

export default function Page() { return <Client />; }
